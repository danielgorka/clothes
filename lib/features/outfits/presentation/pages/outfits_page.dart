import 'package:flutter/material.dart';

class OutfitsPage extends StatelessWidget {
  const OutfitsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Material(
      child: Center(
        child: Text('Outfits page'),
      ),
    );
  }
}
